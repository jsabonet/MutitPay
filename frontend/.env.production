# ====================================
# Frontend Production Environment
# ====================================
# Este arquivo é usado durante o build do Vite para produção

# Use /api para proxy através do Nginx em vez de URL absoluta
# O Nginx vai fazer o proxy de /api/ para http://backend:8000/api/
VITE_API_BASE_URL=/api
